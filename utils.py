import settings

def param_percent(percentage:float,parameter:float) -> float:
    """
    This function calculates the proportion of a value which is called 
    the 'parameter' once we mention the 'percentage' of it.

    output: percentage proportion of the parameter.

    """
    return parameter*percentage/100